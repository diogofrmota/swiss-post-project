# Ingress configuration for Argo CD (disabled until Cilium ingress is fixed)
# Uncomment this section when Cilium ingress controller is working

# server:
#   ingress:
#     enabled: true
#     ingressClassName: cilium
#     annotations:
#       cert-manager.io/cluster-issuer: letsencrypt-prod
#     tls: true