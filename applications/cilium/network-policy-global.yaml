# This policy enforces namespace isolation cluster-wide by blocking all cross-namespace traffic for application pods,
# while allowing same-namespace communication, DNS resolution, and Kubernetes API access — system namespaces are
# excluded since they need broader access by design.
apiVersion: cilium.io/v2
kind: CiliumClusterwideNetworkPolicy
metadata:
  name: deny-cross-namespace
  annotations:
    argocd.argoproj.io/sync-wave: "3" 
spec:
  # Apply to all pods except system namespaces that need cross-namespace access
  endpointSelector:
    matchExpressions:
      - key: k8s:io.kubernetes.pod.namespace
        operator: NotIn
        values:
          - kube-system
          - kyverno
          - argocd
          - monitoring
          - metallb-system
          - cert-manager
  ingress:
    - fromEndpoints:
        - matchExpressions:
            # Allow traffic only from pods in the same namespace
            - key: k8s:io.kubernetes.pod.namespace
              operator: In
              values: []   # Cilium resolves this to the endpoint's own namespace at runtime
  egress:
    # Allow egress to the Kubernetes API server
    - toEntities:
        - kube-apiserver
    # Allow DNS via CoreDNS
    - toEndpoints:
        - matchLabels:
            k8s:io.kubernetes.pod.namespace: kube-system
            k8s:k8s-app: kube-dns
      toPorts:
        - ports:
            - port: "53"
              protocol: UDP
            - port: "53"
              protocol: TCP