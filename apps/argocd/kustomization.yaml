apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: argocd

resources:
- namespace.yaml
- https://raw.githubusercontent.com/argoproj/argo-cd/v3.1.13/manifests/install.yaml
- httproute.yaml
- appset-cilium.yaml
- appset-apps.yaml

configMapGenerator:
- name: argocd-cm
  literals:
  - kustomize.buildOptions=--enable-alpha-plugins --enable-exec
  behavior: merge
- name: argocd-cmd-params-cm
  literals:
  - server.insecure=true
  behavior: merge