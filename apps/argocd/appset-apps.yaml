apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: apps
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  description: "Major components"
  sourceRepos:
    - "https://github.com/diogofrmota/swiss-post-project"
  destinations:
    - namespace: '*'
      server: https://kubernetes.default.svc
  clusterResourceWhitelist:
    - group: '*'
      kind: '*'
  namespaceResourceWhitelist:
    - group: '*'
      kind: '*'
---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: apps
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
  - git:
      repoURL: https://github.com/diogofrmota/swiss-post-project
      revision: HEAD
      directories:
      - path: 'apps/**'
  template:
    metadata:
      name: '{{.path.basenameNormalized}}'
    spec:
      project: "apps"
      source:
        repoURL: https://github.com/diogofrmota/swiss-post-project
        targetRevision: HEAD
        path: '{{.path.path}}'
      destination:
        server: https://kubernetes.default.svc
        namespace: '{{.path.basenameNormalized}}'
      ignoreDifferences:
      - group: cert-manager.io
        kind: ClusterIssuer
        jsonPointers:
        - /spec/acme/solvers/0/dns01/cloudflare/apiTokenSecretRef/namespace
      syncPolicy:
        syncOptions:
        - CreateNamespace=true
        - PruneLast=true
        - PrunePropagationPolicy=foreground
        - Replace=false
        - PreserveResourcesOnDeletion=true