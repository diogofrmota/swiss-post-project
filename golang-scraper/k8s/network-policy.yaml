# Default deny all
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: default-deny
  namespace: hn-scraper
spec:
  endpointSelector: {}
  ingress: []
  egress: []
---
# Allow Prometheus to scrape
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: allow-prometheus-scrape
  namespace: hn-scraper
spec:
  endpointSelector:
    matchLabels:
      app: hn-scraper
  ingress:
    - fromEndpoints:
        - matchLabels:
            app.kubernetes.io/name: prometheus
      toPorts:
        - ports:
            - port: "8080"
              protocol: TCP
---
# Allow egress to the HN API (HTTPS)
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: allow-hn-api
  namespace: hn-scraper
spec:
  endpointSelector:
    matchLabels:
      app: hn-scraper
  egress:
    - toFQDNs:
        - matchName: "hacker-news.firebaseio.com"
      toPorts:
        - ports:
            - port: "443"
              protocol: TCP
---
# Allow DNS
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: allow-dns
  namespace: hn-scraper
spec:
  endpointSelector:
    matchLabels:
      app: hn-scraper
  egress:
    - toEndpoints:
        - matchLabels:
            k8s:io.kubernetes.pod.namespace: kube-system
            k8s:k8s-app: kube-dns
      toPorts:
        - ports:
            - port: "53"
              protocol: UDP
            - port: "53"
              protocol: TCP
          rules:
            dns:
              - matchPattern: "*"